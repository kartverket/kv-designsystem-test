import type { FunctionComponent } from 'react';
import type { BreadcrumbsProps } from '../Breadcrumbs';

/**
 * This component exists so that Storybook will display all component props under their controls
 */
export const Breadcrumbs: FunctionComponent<BreadcrumbsProps> = () => null;
