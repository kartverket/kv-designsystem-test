{
  "name": "@kv-designsystem/tokens",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./dist/green.css",
    "./*": "./dist/*"
  },
  "scripts": {
    "create": "designsystemet tokens create --clean",
    "prebuild": "pnpm run create",
    "build": "pnpm clean && designsystemet tokens build -t ./src -o ./dist --experimental-tailwind",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@digdir/designsystemet": "catalog:",
    "rimraf": "catalog:"
  }
}
